import at.klickverbot.event.IEventDispatcher;

/**
 * Marker interface for collections that dispatch
 * <code>CollectionEvent</code>-type events when changed.
 */
interface at.klickverbot.data.ICollection extends IEventDispatcher {
}
